import {StatusBar} from 'react-native';
import React from 'react';

const StatusAppBar = () => {
  return <StatusBar barStyle="light-content" backgroundColor="transparent" translucent={true} />;
};

export default StatusAppBar;
